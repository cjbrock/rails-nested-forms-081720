<h1>Build-your-own sushi!</h1>
<div class="container">
    <div class="row">
        <div class="col-xs-8">
            <br/>
            <p>You name it, we make it. Fill out your preference and our waiter will bring it directly to you.</p>
            <br/>

            <%# if @sushi.errors.any? %>
                <%# @sushi.errors.full_messages.each do | error | %>
                 <!--<h5 class="error"><%#= error %></h5>-->
                <%# end %>
            <%# end %>

            <% if @sushi.errors.any? %>
                 <h5 class="error"><%= @sushi.errors.full_messages.to_sentence %></h5>
            <% end %>


            <%= form_with model: @sushi, local: true do |f| %>
                <%= f.label :name, "Sushi Name" %>
                <%= f.text_field :name, class: @sushi.errors.any? ? "error-field" : nil %>
                <h4>Would you like to add any ingredients?</h4>
                <%= f.fields_for :ingredients do | i | %>
                    <%= i.label :name, "Ingredient Name" %>
                    <%= i.text_field :name %><br />
                    <%= i.label :calories %>
                    <%= i.number_field :calories %><br />
                    <%= i.label :serving_size %>
                    <%= i.number_field :serving_size %><br />
                <% end %>
                <%= f.submit %>
            <% end %> 


        </div>
        <div class="col-xs-4">
            <div class="card" style="width: 20rem;">
                <img class="card-img-top img-responsive" src="https://i.pinimg.com/736x/58/b1/d7/58b1d789616aa6ef4ae6b56cf86af16b--coffee-geek-drink-coffee.jpg"/>
                <div class="card-block">
                    <p class="card-text">Every sushi order comes with a cup of damn fine coffee.</p>
                </div>
            </div>
        </div>
    </div>
</div>
